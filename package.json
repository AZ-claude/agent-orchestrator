{
  "name": "agent-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Pilot-only local daemon scaffolding for the Agent Orchestrator v1.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test dist/test/config.test.js && :"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
